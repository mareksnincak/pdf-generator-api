export enum TemplateType {
  htmlHandlebars = 'html/handlebars',
}

export enum MalwareScanStatus {
  clean = 'clean',
  failed = 'failed',
  infected = 'infected',
  pending = 'pending',
  skipped = 'skipped',
}
